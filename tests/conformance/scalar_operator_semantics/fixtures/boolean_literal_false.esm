{
  "esm": "1.1.0",
  "metadata": {
    "name": "FalseOp",
    "description": "The `false` literal operator of esm-spec \u00a74.2's evaluable core, on its own: 0 in the numeric boolean encoding every comparison produces. It was split out of `scalar_operator_semantics` when two of the three bindings carried no evaluation rule for it (Julia's tree-walk evaluator had one for `true` only, issue #460, and Rust classed `false` as a rewrite target, issue #461). All three evaluate it now.",
    "authors": [
      "EarthSciML Authors and Contributors"
    ],
    "tags": [
      "conformance",
      "scalar_operator_semantics",
      "inline-test",
      "boolean-literal"
    ],
    "license": "MIT"
  },
  "models": {
    "FalseOp": {
      "variables": {
        "literal_false": {
          "type": "unknown",
          "units": "1",
          "description": "The operator under test: The `false` literal operator of esm-spec \u00a74.2's evaluable core, on its own: 0 in the numeric boolean encoding every comparison produces. It was split out of `scalar_operator_semantics` when two of the three bindings carried no evaluation rule for it (Julia's tree-walk evaluator had one for `true` only, issue #460, and Rust classed `false` as a rewrite target, issue #461). All three evaluate it now."
        },
        "probe": {
          "type": "unknown",
          "units": "1",
          "default": 0.0,
          "description": "Integrates the observed, so the value is read as well as computed."
        }
      },
      "equations": [
        {
          "lhs": "literal_false",
          "rhs": {
            "op": "false",
            "args": []
          }
        },
        {
          "lhs": {
            "op": "D",
            "args": [
              "probe"
            ],
            "wrt": "t"
          },
          "rhs": "literal_false"
        }
      ],
      "tests": [
        {
          "id": "literal_false_value",
          "description": "The `false` literal operator of esm-spec \u00a74.2's evaluable core, on its own: 0 in the numeric boolean encoding every comparison produces. It was split out of `scalar_operator_semantics` when two of the three bindings carried no evaluation rule for it (Julia's tree-walk evaluator had one for `true` only, issue #460, and Rust classed `false` as a rewrite target, issue #461). All three evaluate it now.",
          "time_span": {
            "start": 0.0,
            "end": 1.0
          },
          "tolerance": {
            "rel": 0.0,
            "abs": 0.0
          },
          "assertions": [
            {
              "variable": "literal_false",
              "time": 0.0,
              "expected": 0.0
            },
            {
              "variable": "probe",
              "time": 1.0,
              "expected": 0.0,
              "tolerance": {
                "rel": 1e-09,
                "abs": 1e-11
              }
            }
          ]
        }
      ]
    }
  }
}
