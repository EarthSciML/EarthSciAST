{
  "esm": "1.0.0",
  "metadata": {
    "name": "MountEnumsLeafTwo",
    "description": "A leaf whose own `enums` block says probe.Symbol = 2, read back by an `enum` op. Mounted, it must still read 2 (esm-spec §9.3).",
    "authors": [
      "EarthSciML Authors and Contributors"
    ]
  },
  "enums": {
    "probe": {
      "Symbol": 2
    }
  },
  "models": {
    "LeafTwo": {
      "variables": {
        "value": {
          "type": "unknown",
          "units": "1",
          "description": "The value this file's own `enums` block gives probe.Symbol."
        }
      },
      "equations": [
        {
          "lhs": "value",
          "rhs": {
            "op": "enum",
            "args": [
              "probe",
              "Symbol"
            ]
          }
        }
      ]
    }
  }
}
