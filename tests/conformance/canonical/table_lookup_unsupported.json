{
  "id": "table_lookup_unsupported",
  "description": "Fail-closed: a table_lookup node carries table/axes, neither of which is in the canonical emissible set {op,args,wrt,dim,fn,name,value}. canonical_json must raise E_CANONICAL_UNSUPPORTED_FIELD.",
  "input": {
    "op": "table_lookup",
    "args": [],
    "table": "tableA",
    "axes": {"code": "fm"}
  },
  "expect_error": "E_CANONICAL_UNSUPPORTED_FIELD",
  "tags": ["fail_closed", "table_lookup", "table", "axes"]
}
