# Rust
target/
# Secondary target dir for RUSTFLAGS=-C target-cpu=native A/B builds
# (step 4b ceiling measurements) — never commit build artifacts.
target-native/
# Cargo.lock is committed: this crate ships the `esm` binary (see the note in
# the repository .gitignore).
**/*.rs.bk

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# WASM
pkg/
wasm-pack.log

# Distribution
dist/

# A/B and opt-variant build dirs used by the conformance timing probes
target-opt/
target-pkg2/
target-esio/
